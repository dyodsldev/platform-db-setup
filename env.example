# ================================================================
# Environment Configuration Template
# Copy this file to .env and fill in your values
# ================================================================

# Database Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dyodsl-dev
DB_USER=postgres
DB_PASSWORD=your_password_here

# Database URL (constructed from above)
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Flyway Configuration
FLYWAY_URL=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
FLYWAY_USER=${DB_USER}
FLYWAY_PASSWORD=${DB_PASSWORD}

# Application Settings
APP_USER=healthcare_app
SCHEMA_VERSION=1.0

# Environment
ENVIRONMENT=dev

# Optional: For production/staging
# DB_HOST=production-db.example.com
# DB_SSL_MODE=require
# FLYWAY_BASELINE_VERSION=1

# Optional: Neon Database (if using)
# DATABASE_URL=postgresql://user:pass@ep-cool-moon-123456.us-east-2.aws.neon.tech/dbname?sslmode=require
